{
  "name": "start-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "bootstrap": "pnpm install && node scripts/sync-opensrc.mjs --soft-fail",
    "build": "turbo run build",
    "check": "turbo run check",
    "db:down": "node scripts/dev/local-postgres.mjs down",
    "db:ensure": "node scripts/dev/local-postgres.mjs ensure",
    "db:logs": "node scripts/dev/local-postgres.mjs logs",
    "db:reset": "node scripts/dev/local-postgres.mjs reset",
    "db:url": "node scripts/dev/local-postgres.mjs url",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "fix": "pnpm exec ultracite fix && pnpm exec oxfmt --write .",
    "format": "pnpm fix",
    "lint": "pnpm check",
    "opensrc:sync": "node scripts/sync-opensrc.mjs",
    "precheck": "pnpm exec ultracite check package.json turbo.json tsconfig.json .oxlintrc.json opencode.json scripts",
    "test:db-infra": "node --test scripts/lib/local-postgres-config.test.mjs scripts/lib/local-postgres.test.mjs",
    "test:opensrc": "node --test scripts/lib/opensrc-sync.test.mjs",
    "test:ui-propagation": "node --test scripts/lib/ui-propagation.test.mjs",
    "verify:ui-propagation": "node scripts/verify-ui-propagation.mjs",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "opensrc": "0.6.0",
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0",
    "playwright-core": "^1.58.2",
    "turbo": "^2.8.8",
    "typescript": "5.9.3",
    "ultracite": "^7.3.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.32.1"
}
